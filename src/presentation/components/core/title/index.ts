import { TitleItem } from './title-item';
import { TitleRoot } from './title-root';
import { TitleText } from './title-text';

export const Title = {
  Root: TitleRoot,
  Text: TitleText,
  Item: TitleItem,
};
